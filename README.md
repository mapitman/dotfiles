# dotfiles

Personal dotfiles managed with [YADM](https://yadm.io/). The working tree is `$HOME` — tracked files live at their real paths.

## Setup

```sh
yadm clone git@github.com:mapitman/dotfiles.git
```

